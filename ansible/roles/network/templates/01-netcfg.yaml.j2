network:
  version: 2
  renderer: networkd
  ethernets:
    {{ network_interface }}:
      addresses:
        - {{ host_ip }}/{{ netmask_cidr }}
      routes:
        - to: default
          via: {{ gateway }}
      nameservers:
        addresses:
{% for dns in dns_servers %}
          - {{ dns }}
{% endfor %}